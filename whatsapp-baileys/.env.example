# URL of the RAG service (no trailing slash needed).
# The bot POSTs to {RAG_URL}/widget/chat
RAG_URL=http://127.0.0.1:8000

# Shared secret sent as the X-API-Key header. Must match WIDGET_API_KEY on the
# RAG service. Leave empty only if the RAG service runs without a key set.
RAG_API_KEY=
