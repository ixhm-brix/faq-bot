# Munyakazi — briqx website assistant.
#
# No sentence-transformers / chromadb / torch: the knowledge base is ~1.9k tokens
# and is sent whole in the cached system prompt, so there is no retrieval step and
# nothing to embed. That is what lets this run on a 1GB VPS.
# No aiogram: Telegram is retired, the widget is the only channel.
python-dotenv==1.0.1
openai>=1.55.3
fastapi>=0.110.0
python-multipart>=0.0.9
itsdangerous>=2.0.0
jinja2>=3.1.0
bcrypt>=4.1
httpx>=0.27
uvicorn>=0.30
dateparser>=1.2
reportlab>=4.0.0
